import { TargetConfiguration, Tree } from '@nx/devkit';
import { LibraryGeneratorSchema } from './schema';
export declare const defaultTargets: {
    [targetName: string]: TargetConfiguration;
};
export default function libraryGenerator(tree: Tree, schema: LibraryGeneratorSchema): Promise<void>;
